class Solution {
    public void rotate(int[][] matrix) {
        int N = matrix.length;
       for (int j = 0; j < N; j++)
        {
            for (int i = N - 1; i >= 0; i--)
                System.out.print(matrix[i][j] + " ");
            System.out.println();
        }
    }
}
